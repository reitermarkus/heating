#!/bin/bash

set -e
set -o pipefail
set -o nounset

ip="${1}"

TARGET='arm-unknown-linux-gnueabihf'

cross build --release --target "${TARGET}"
rsync -avz "target/${TARGET}/release/" pi@"${ip}:heating/"
ssh pi@"${ip}" -t ./heating/heating
